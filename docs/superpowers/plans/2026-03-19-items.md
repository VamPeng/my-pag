# Items API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement V1 `items` backend APIs (CRUD + complete + trash/restore + basic filters) aligned with requirements and technical design.

**Architecture:** Add a dedicated `item` module in server with controller/service/repository layering, reusing the existing single-account model. Keep persistence logic in JDBC repository and business rules in service (validation, completion semantics, directory checks). Extend integration tests first (RED), then implement minimal code for GREEN.

**Tech Stack:** Java 17, Spring Boot Web + JDBC, SQLite, JUnit 5 + MockMvc.

---

### Task 1: API Contract Tests First (TDD RED)

**Files:**
- Modify: `apps/server/src/test/java/com/vampeng/mypag/api/ApiIntegrationTest.java`

- [x] **Step 1: Add failing tests for `POST/GET/PATCH /api/items`**
- [x] **Step 2: Add failing tests for `POST /api/items/:id/complete` and completion rollback via `PATCH`**
- [x] **Step 3: Add failing tests for `POST /api/items/:id/trash` and `POST /api/items/:id/restore`**
- [x] **Step 4: Add failing tests for `GET /api/items` filters (`q`, `progress`, `priority`, `directoryId`)**
- [x] **Step 5: Run `GRADLE_USER_HOME=/Users/yuhuipeng/Desktop/pro/webP/my-pag/apps/server/.gradle ./gradlew test --tests com.vampeng.mypag.api.ApiIntegrationTest` and verify RED**

### Task 2: Implement Item Module (GREEN)

**Files:**
- Create: `apps/server/src/main/java/com/vampeng/mypag/item/ItemController.java`
- Create: `apps/server/src/main/java/com/vampeng/mypag/item/ItemService.java`
- Create: `apps/server/src/main/java/com/vampeng/mypag/item/ItemRepository.java`

- [x] **Step 1: Implement repository SQL for create/get/list/update/complete/trash/restore**
- [x] **Step 2: Implement service validation and business semantics (title required, completion timestamps, directory checks)**
- [x] **Step 3: Implement controller endpoints matching technical-design API paths**
- [x] **Step 4: Run targeted integration tests and verify GREEN**

### Task 3: Full Verification

**Files:**
- Modify: `docs/superpowers/plans/2026-03-19-items.md`

- [x] **Step 1: Run full backend tests**
Run: `GRADLE_USER_HOME=/Users/yuhuipeng/Desktop/pro/webP/my-pag/apps/server/.gradle ./gradlew test`
Expected: `BUILD SUCCESSFUL`

- [x] **Step 2: Run frontend build regression check**
Run: `npm run build`
Expected: build success

- [x] **Step 3: Mark plan checkboxes and commit**
```bash
git add apps/server docs/superpowers/plans/2026-03-19-items.md
git commit -m "feat: implement items apis"
```
