# Views API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement V1 smart-view endpoints (`inbox/today/upcoming/overdue`) for server-side time-based access.

**Architecture:** Add a dedicated `view` module with controller/service/repository layering and keep view selection logic in repository SQL filters. Reuse existing account bootstrap and settings configuration so upcoming-range is driven by `settings.recent_range_value` and `settings.recent_range_unit`. Extend integration tests first (RED), then implement minimal endpoint code for GREEN.

**Tech Stack:** Java 17, Spring Boot Web + JDBC, SQLite, JUnit 5 + MockMvc.

---

### Task 1: View API Contract Tests First (TDD RED)

**Files:**
- Modify: `apps/server/src/test/java/com/vampeng/mypag/api/ApiIntegrationTest.java`

- [x] **Step 1: Add failing test for `GET /api/views/inbox`**
- [x] **Step 2: Add failing test for `GET /api/views/today`**
- [x] **Step 3: Add failing test for `GET /api/views/upcoming` (settings-driven range)**
- [x] **Step 4: Add failing test for `GET /api/views/overdue` (exclude done)**
- [x] **Step 5: Run `GRADLE_USER_HOME=/Users/yuhuipeng/Desktop/pro/webP/my-pag/apps/server/.gradle ./gradlew test --tests com.vampeng.mypag.api.ApiIntegrationTest` and verify RED**

### Task 2: Implement View Module (GREEN)

**Files:**
- Create: `apps/server/src/main/java/com/vampeng/mypag/view/ViewController.java`
- Create: `apps/server/src/main/java/com/vampeng/mypag/view/ViewService.java`
- Create: `apps/server/src/main/java/com/vampeng/mypag/view/ViewRepository.java`

- [x] **Step 1: Implement repository queries for inbox/today/upcoming/overdue**
- [x] **Step 2: Implement service composition with settings-based upcoming range**
- [x] **Step 3: Implement controller endpoints matching technical-design contract**
- [x] **Step 4: Run targeted integration tests and verify GREEN**

### Task 3: Full Verification

**Files:**
- Modify: `docs/superpowers/plans/2026-03-19-views.md`

- [x] **Step 1: Run full backend tests**
Run: `GRADLE_USER_HOME=/Users/yuhuipeng/Desktop/pro/webP/my-pag/apps/server/.gradle ./gradlew test`
Expected: `BUILD SUCCESSFUL`

- [x] **Step 2: Run frontend build regression check**
Run: `npm run build`
Expected: build success

- [x] **Step 3: Mark plan checkboxes and commit**
```bash
git add apps/server docs/superpowers/plans/2026-03-19-views.md
git commit -m "feat: implement smart view apis"
```
